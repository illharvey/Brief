---
phase: 05.1-settings-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/dashboard/layout.tsx
  - src/app/dashboard/page.tsx
  - src/components/dashboard/sidebar.tsx
autonomous: true
requirements:
  - PREF-03

must_haves:
  truths:
    - "Design system color tokens (milk, espresso, beeswax, beeswax-deep, warm-brown, steam, cream) are available as Tailwind utility classes"
    - "Playfair Display and DM Sans fonts are loaded and available via CSS variables --font-playfair-display and --font-dm-sans"
    - "All /dashboard/* routes render inside a two-column sidebar shell (espresso sidebar + milk main area with grain overlay)"
    - "The DashboardSidebar shows the Brief. logo, Briefings nav link (muted), Settings nav link (beeswax accent), user email, and Sign out button"
    - "dashboard/page.tsx no longer has a duplicate sign-out button"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brief design system tokens in @theme block"
      contains: "--color-milk"
    - path: "src/app/layout.tsx"
      provides: "Playfair Display + DM Sans font loading"
      contains: "Playfair_Display"
    - path: "src/components/dashboard/sidebar.tsx"
      provides: "DashboardSidebar component"
      exports: ["DashboardSidebar"]
    - path: "src/app/dashboard/layout.tsx"
      provides: "Two-column flex layout with sidebar"
      contains: "DashboardSidebar"
  key_links:
    - from: "src/app/dashboard/layout.tsx"
      to: "src/components/dashboard/sidebar.tsx"
      via: "DashboardSidebar import"
      pattern: "DashboardSidebar"
    - from: "src/app/globals.css"
      to: "Tailwind utilities"
      via: "@theme inline block"
      pattern: "--color-milk"
---

<objective>
Add the Brief design system tokens to globals.css, load Playfair Display and DM Sans via next/font, and replace the minimal dashboard shell with a full sidebar layout.

Purpose: Every downstream task (Plan 02 settings page) needs the design tokens available as Tailwind utilities and the sidebar shell in place. This is the non-deferrable foundation for the entire settings page feature.
Output: Functional sidebar dashboard shell visible at /dashboard, design system color tokens active, editorial fonts loaded.
</objective>

<execution_context>
@/Users/willharveynats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willharveynats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-settings-page/05.1-CONTEXT.md
@.planning/phases/05.1-settings-page/05.1-RESEARCH.md

<interfaces>
<!-- Key types and patterns the executor needs. -->

From src/app/globals.css (current @theme block — EXTEND this, do not replace):
```css
@theme inline {
  --color-background: var(--background);
  /* ... existing shadcn tokens ... */
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}
/* :root { --radius: 0.625rem; ... shadcn colors ... } */
```
IMPORTANT: Add Brief tokens INSIDE the existing @theme inline block. Do NOT create a second @theme block. Do NOT rename or shadow existing shadcn tokens. Use --color-brief-muted (not --color-muted) for the #7A5C3A warm-muted color to avoid collision with shadcn's --muted.

From src/app/layout.tsx (current — UPDATE this):
```typescript
import { Geist, Geist_Mono } from "next/font/google"
// body className={`${geistSans.variable} ${geistMono.variable} antialiased`}
```
Add Playfair_Display and DM_Sans imports alongside Geist. Append their variables to the body className.

From src/app/dashboard/layout.tsx (current — REPLACE body):
```typescript
return <div className="min-h-screen bg-background">{children}</div>
```
Replace the wrapper div with the two-column flex layout. The auth check (session + redirect) must be preserved.

From src/app/dashboard/page.tsx (current — SIMPLIFY):
The page embeds a sign-out form as placeholder nav. Remove the sign-out form and its imports when the sidebar takes over sign-out. Keep the Phase 6 placeholder text. The page must remain a valid Server Component.

From src/actions/auth.ts — signOutAction is already exported; the sidebar will import it for its sign-out button.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Brief design system tokens and load editorial fonts</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
  </files>
  <action>
    **globals.css:** Inside the existing `@theme inline { ... }` block, append the following Brief design system tokens AFTER the existing entries (do not create a second @theme block):

    ```css
    /* Brief design system */
    --color-milk: #FDF9F0;
    --color-espresso: #1C0F05;
    --color-beeswax: #C1522A;
    --color-beeswax-deep: #A33E1A;
    --color-warm-brown: #5C3D1E;
    --color-brief-muted: #7A5C3A;
    --color-steam: #F0E8D5;
    --color-cream: #FAF4E8;

    /* Editorial font variables */
    --font-playfair: var(--font-playfair-display);
    --font-dm-sans: var(--font-dm-sans);
    ```

    Do NOT add --color-muted (it shadows shadcn's gray muted). Use --color-brief-muted for #7A5C3A.

    **layout.tsx:** Add Playfair_Display and DM_Sans imports from next/font/google alongside the existing Geist imports. Configure them with CSS variables. Append both variables to the body className.

    ```typescript
    import { Geist, Geist_Mono, Playfair_Display, DM_Sans } from "next/font/google"

    const playfairDisplay = Playfair_Display({
      subsets: ["latin"],
      variable: "--font-playfair-display",
      display: "swap",
    })

    const dmSans = DM_Sans({
      subsets: ["latin"],
      variable: "--font-dm-sans",
      display: "swap",
    })
    ```

    Body className: `${geistSans.variable} ${geistMono.variable} ${playfairDisplay.variable} ${dmSans.variable} antialiased`

    Also update the metadata title from "Create Next App" to "Brief" and description to "Your daily personalised briefing."
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>
    - globals.css @theme block contains --color-milk, --color-espresso, --color-beeswax, --color-beeswax-deep, --color-warm-brown, --color-brief-muted, --color-steam, --color-cream, --font-playfair, --font-dm-sans
    - layout.tsx imports Playfair_Display and DM_Sans, exposes them as CSS variables on body
    - No TypeScript errors
    - shadcn --muted token NOT shadowed (--color-muted not added)
  </done>
</task>

<task type="auto">
  <name>Task 2: Build DashboardSidebar and replace dashboard shell layout</name>
  <files>
    src/components/dashboard/sidebar.tsx
    src/app/dashboard/layout.tsx
    src/app/dashboard/page.tsx
  </files>
  <action>
    **Create src/components/dashboard/sidebar.tsx** — a Client Component (needs "use client" for the sign-out form action):

    Structure:
    - Fixed sidebar: `w-64 min-h-screen bg-espresso flex flex-col`
    - Top: "Brief." logo — Playfair Display 900, beeswax-colored period. Example: `<span className="font-playfair font-black text-milk">Brief<span className="text-beeswax-deep">.</span></span>`
    - Nav section (middle, flex-1): two links
      - "Briefings" — `/dashboard` — muted style (text-brief-muted or similar; not active yet, Phase 6 placeholder). Use `<Link>` from next/link.
      - "Settings" — `/dashboard/settings` — active style: beeswax accent left border + beeswax text. Use `<Link>`.
    - Bottom: user email (text-brief-muted text-sm truncate) + sign-out form with signOutAction. The sign-out button: `variant="ghost"`, small, text-brief-muted hover:text-milk.

    Props: `{ userEmail: string }`

    Use `usePathname()` from next/navigation to detect the active route and apply the beeswax accent style to the correct nav link.

    **Update src/app/dashboard/layout.tsx** — replace the wrapper div with a two-column flex layout. Keep the auth check:

    ```typescript
    import { DashboardSidebar } from "@/components/dashboard/sidebar"

    // Replace the return:
    return (
      <div className="flex min-h-screen">
        <DashboardSidebar userEmail={session.user.email ?? ""} />
        <main className="flex-1 bg-milk relative overflow-auto">
          {/* Grain texture overlay — fixed, pointer-events-none */}
          <div
            aria-hidden
            className="pointer-events-none fixed inset-0 z-0"
            style={{ opacity: 0.4 }}
          >
            <svg width="100%" height="100%" xmlns="http://www.w3.org/2000/svg">
              <filter id="brief-grain">
                <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch" />
                <feColorMatrix type="saturate" values="0" />
              </filter>
              <rect width="100%" height="100%" filter="url(#brief-grain)" />
            </svg>
          </div>
          <div className="relative z-10">{children}</div>
        </main>
      </div>
    )
    ```

    **Update src/app/dashboard/page.tsx** — remove the sign-out form and its imports (signOutAction, Button). The sidebar now handles sign-out. Replace with a simple placeholder that still reads the session for the email:

    ```typescript
    import { auth } from "@/lib/auth"

    export default async function DashboardPage() {
      const session = await auth()

      return (
        <div className="p-8">
          <h1 className="text-2xl font-semibold mb-2">Dashboard</h1>
          <p className="text-muted-foreground text-sm">
            Logged in as <strong>{session?.user?.email}</strong>
          </p>
          <p className="text-sm text-muted-foreground mt-2">
            Phase 6 will replace this placeholder with the full briefing viewer.
          </p>
        </div>
      )
    }
    ```
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>
    - src/components/dashboard/sidebar.tsx exists, exports DashboardSidebar, accepts userEmail prop
    - dashboard/layout.tsx renders DashboardSidebar + main with grain overlay and z-10 content wrapper
    - dashboard/page.tsx no longer imports signOutAction or Button; no duplicate sign-out form
    - No TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `src/components/dashboard/sidebar.tsx` exists
- globals.css @theme inline block contains --color-milk
- layout.tsx body className includes playfairDisplay.variable and dmSans.variable
- dashboard/layout.tsx contains "DashboardSidebar"
- dashboard/page.tsx does NOT contain "signOutAction"
</verification>

<success_criteria>
1. Brief design system tokens are available as Tailwind utilities (bg-milk, text-espresso, bg-beeswax, etc.)
2. Playfair Display and DM Sans fonts are loaded and usable via font-playfair and font-dm-sans Tailwind classes
3. Every /dashboard/* route renders inside the espresso sidebar shell with milk main area and grain overlay
4. DashboardSidebar shows logo, nav links, user email, and sign-out — no duplicate sign-out on dashboard/page.tsx
5. TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-settings-page/05.1-01-SUMMARY.md` following the summary template.
</output>
