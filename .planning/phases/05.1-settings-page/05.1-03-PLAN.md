---
phase: 05.1-settings-page
plan: "03"
type: execute
wave: 3
depends_on:
  - "05.1-02"
files_modified: []
autonomous: false
requirements:
  - PREF-03

must_haves:
  truths:
    - "A logged-in user can visit /dashboard/settings and see their existing topics and delivery time"
    - "A user can add a topic, remove a topic, and change the delivery time, then save — all changes persist after page refresh"
    - "The 'Preferences' link in a briefing email resolves to /dashboard/settings with no 404"
    - "Toast notifications appear on save success and failure"
    - "The sidebar renders correctly with espresso background, Brief. logo, and active Settings link highlighted"
  artifacts:
    - path: "src/app/dashboard/settings/page.tsx"
      provides: "Live /dashboard/settings route"
    - path: "src/app/dashboard/settings/settings-form.tsx"
      provides: "Working save form with toast"
    - path: "src/components/dashboard/sidebar.tsx"
      provides: "Sidebar with correct visual design"
  key_links:
    - from: "briefing email 'Preferences' link"
      to: "/dashboard/settings"
      via: "APP_URL/dashboard/settings in email.ts"
      pattern: "preferencesUrl"
---

<objective>
Human verification of the complete Phase 5.1 settings page feature against the live development server.

Purpose: Confirm that all four success criteria from the phase goal are met: user can view preferences, save changes, the email link resolves, and the visual design matches the locked decisions.
Output: Phase 5.1 confirmed complete; PREF-03 satisfied.
</objective>

<execution_context>
@/Users/willharveynats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willharveynats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05.1-settings-page/05.1-CONTEXT.md
@.planning/phases/05.1-settings-page/05.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm TypeScript build passes</name>
  <files></files>
  <action>
    Run a type check from the project root to confirm no TypeScript errors remain before handing off to user verification:

    ```bash
    cd /Users/willharveynats/conductor/repos/brief
    npx tsc --noEmit
    ```

    If errors exist, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/willharveynats/conductor/repos/brief && npx tsc --noEmit 2>&1; echo "Exit: $?"</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits with code 0
    - No TypeScript errors in any of the new files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete settings page feature</name>
  <files></files>
  <action>
    Start the dev server (`npm run dev` in the project root), then verify the complete Phase 5.1 feature against the live development server at http://localhost:3000.

    What was built:
    - Dashboard sidebar shell (espresso background, Brief. logo, nav links, user email, sign-out)
    - Grain texture overlay on milk main content area
    - /dashboard/settings page with current topics pre-populated as pills, delivery time dropdown pre-selected
    - Add/remove topics via freeform input and suggestion chips
    - 17-slot delivery time dropdown (6 AM to 10 PM, 1-hour increments) showing detected timezone
    - Single "Save preferences" button saving both topics and delivery time atomically
    - Toast notifications: "Preferences saved" on success, "Failed to save. Try again." on failure
    - Playfair Display heading + DM Sans subtitle on settings page

    Verify all four phase success criteria:

    **1. View current preferences at /dashboard/settings**
    - Log in with your account at http://localhost:3000/login
    - Navigate to http://localhost:3000/dashboard/settings
    - Confirm: existing topics appear as removable pills
    - Confirm: saved delivery time is pre-selected in the dropdown
    - Confirm: sidebar shows espresso background, "Brief." logo (beeswax period), "Settings" link with active beeswax accent, user email at the bottom, and a Sign out button
    - Confirm: grain texture is visible over the milk main area
    - Confirm: page heading "Settings" is in Playfair Display serif

    **2. Update topics and save**
    - Add a new topic (type it in the input and press Enter, or click a suggestion chip)
    - Remove an existing topic (click the X button on a pill)
    - Click "Save preferences"
    - Confirm: "Preferences saved" toast appears
    - Refresh the page — confirm topic changes persisted

    **3. Update delivery time and save**
    - Change the delivery time dropdown to a different time
    - Click "Save preferences"
    - Confirm: "Preferences saved" toast appears
    - Refresh the page — confirm new delivery time is still selected
    - Confirm: local timezone is shown next to the dropdown label

    **4. Email "Preferences" link resolves**
    - Visit http://localhost:3000/dashboard/settings directly
    - Confirm: page loads with no 404
  </action>
  <verify>
    <automated>MISSING — this task requires human visual and functional verification in the browser</automated>
  </verify>
  <done>User types "approved" after confirming all four criteria pass.</done>
</task>

</tasks>

<verification>
All four phase success criteria confirmed by user:
1. User can navigate to /dashboard/settings and see current topics and delivery time
2. User can update topics and save — changes persist after refresh
3. User can update delivery time and save — dispatch will respect the new time
4. /dashboard/settings resolves without 404 (email "Preferences" link works)
</verification>

<success_criteria>
Phase 5.1 is complete when the user types "approved" after verifying all four success criteria above. PREF-03 is satisfied.
</success_criteria>

<output>
After user approves, create `.planning/phases/05.1-settings-page/05.1-03-SUMMARY.md` following the summary template.

Also update `.planning/ROADMAP.md`: mark Phase 5.1 as complete with today's date.
Also update `.planning/STATE.md`: set current position to Phase 5.1 complete, update progress metrics.
Also update `.planning/REQUIREMENTS.md`: mark PREF-03 as [x] complete.
</output>
