---
phase: 07-beta-polish
plan: "05"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "10 or more non-cached briefing items have been reviewed with their source snapshots"
    - "Zero fabricated facts found — every specific claim (names, numbers, events) is traceable to the source snapshot text"
    - "If any hallucination is found, the summarisation prompt is fixed and a new 10+ sample audit is run before this gate passes"
  artifacts: []
  key_links:
    - from: "scripts/audit-briefing.ts"
      to: "briefingItems + articles tables"
      via: "DB query with fromCache=false filter"
      pattern: "fromCache.*false"
---

<objective>
Run the AI hallucination audit using the audit-briefing.ts script from Plan 02. Review 10+ non-cached briefing items by comparing each AI summary claim against its sourceSnapshot.

Purpose: Gate criterion 4 (AI summaries pass a manual spot-check with zero fabricated facts) must be satisfied before beta users are onboarded. This is a human verification task — Claude generates the audit document, developer reads and signs off each item.

Output: Gate criterion 4 signed off, or a blocking hallucination identified and remediated.
</objective>

<execution_context>
@/Users/willharveynats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willharveynats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-beta-polish/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 output -->
scripts/audit-briefing.ts — Usage: npx tsx scripts/audit-briefing.ts <briefingId>
scripts/briefing-count.ts — Usage: npx tsx scripts/briefing-count.ts [--days N]
scripts/generate-briefing.ts — Usage: npx tsx scripts/generate-briefing.ts <userId>

Key constraint from research: briefingItems.fromCache = true means sourceSnapshot is '' (empty string).
The audit script already filters fromCache=false — only non-cached items are included.
If fewer than 10 non-cached items are available, generate a fresh briefing after clearing Redis cache for the test user.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate audit document(s)</name>
  <files></files>
  <action>
    Run the following steps to produce the audit document. Print the output to a file for the human reviewer.

    1. Find a recent briefing ID:
       ```
       npx tsx scripts/briefing-count.ts --days 7
       ```
       This shows recent sent briefings with their delivery dates.

    2. Get a briefingId from the database. Connect via Neon console or run:
       ```
       npx tsx -e "import 'dotenv/config'; import { db } from './src/lib/db/client'; import { briefings } from './src/lib/db/schema'; import { desc } from 'drizzle-orm'; db.select().from(briefings).orderBy(desc(briefings.createdAt)).limit(5).then(r => console.log(JSON.stringify(r, null, 2)))"
       ```

    3. Generate the audit doc:
       ```
       npx tsx scripts/audit-briefing.ts <briefingId> > /tmp/audit-{briefingId}.md
       ```

    4. Check the item count in the output. If fewer than 10 non-cached items are returned, the script will print a warning. In that case:
       a. Clear Redis cache for the audit user (connect to Upstash Redis console and flush or use: `npx tsx -e "import 'dotenv/config'; import { redis } from './src/lib/redis'; redis.flushall().then(console.log)"`)
       b. Re-run generate-briefing.ts: `npx tsx scripts/generate-briefing.ts <userId>`
       c. Re-run the audit script with the new briefingId.

    5. If 10+ non-cached items are available, print the audit doc path so the reviewer can open it:
       ```
       cat /tmp/audit-{briefingId}.md
       ```

    Report: total items found, how many are non-cached, briefingId used.
  </action>
  <verify>
    <automated>ls /tmp/audit-*.md 2>/dev/null | head -3</automated>
  </verify>
  <done>Audit Markdown document exists at /tmp/audit-{briefingId}.md with 10+ items listed (non-cached). Document printed to terminal for review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 generated a Markdown audit document at /tmp/audit-{briefingId}.md.
    The document shows each AI summary alongside the exact source text (sourceSnapshot) and article URL that was used to generate it.
  </what-built>
  <how-to-verify>
    Open the audit document:
    ```
    cat /tmp/audit-{briefingId}.md
    ```
    (or open the file in your editor)

    For each item:
    1. Read the **AI summary** claim.
    2. Read the **Source snapshot** excerpt.
    3. If any specific claim (name, number, date, event) in the AI summary cannot be found in the source snapshot, mark it as **FAIL**.
    4. Open the **Article URL** to verify against the original source if the snapshot is ambiguous.

    Acceptance criteria:
    - Zero items marked FAIL (no fabricated facts)
    - 10+ items reviewed

    If any item is marked FAIL:
    - Note the topic and the specific fabricated claim
    - Type "FAIL: {topic} — {what was fabricated}" as your response
    - The plan executor will then fix the summarisation prompt in src/lib/summarisation/llm.ts and re-run the audit

    If all pass:
    - Type "approved" to complete this gate
  </how-to-verify>
  <resume-signal>Type "approved" if all 10+ items pass with zero fabricated facts, or "FAIL: {topic} — {description}" if hallucination found</resume-signal>
</task>

</tasks>

<verification>
Audit document covers 10+ non-cached items. Developer has reviewed each item and confirmed zero fabricated facts. Gate criterion 4 signed off.
</verification>

<success_criteria>
AI hallucination audit complete. 10+ briefing items reviewed. Zero fabricated facts. Gate criterion 4 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-beta-polish/07-05-SUMMARY.md`
</output>
