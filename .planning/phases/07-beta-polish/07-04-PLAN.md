---
phase: 07-beta-polish
plan: "04"
type: execute
wave: 2
depends_on: []
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "The briefing email renders correctly in Gmail web (matches design intent — readable, fonts load, topic sections visible)"
    - "The briefing email renders correctly in Apple Mail on macOS or iOS"
    - "The briefing email renders correctly in Gmail mobile app"
    - "The briefing email renders correctly in Outlook desktop (tested via direct send to Outlook account)"
    - "Mail Tester spam score is >= 9/10"
  artifacts: []
  key_links:
    - from: "src/emails/briefing-email.tsx"
      to: "Mail Tester inbox"
      via: "sendBriefingEmail() to mail-tester address"
      pattern: "mail-tester\\.com"
---

<objective>
Human-verify that the BriefingEmail template renders correctly across all four required email clients by sending a real email via the production Resend configuration and checking it in each client.

Purpose: Gate criterion 1 (email renders correctly in Gmail, Apple Mail, Outlook, Gmail mobile) is a human verification step that cannot be automated. No code changes are made in this plan — it is purely a testing checkpoint.

Output: All four clients confirmed. Gate criterion 1 signed off.
</objective>

<execution_context>
@/Users/willharveynats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willharveynats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- What the tester needs to know -->
The BriefingEmail React Email template is at: src/emails/briefing-email.tsx
The send function is: sendBriefingEmail(email, userName, topicSections, displayDate) in src/lib/email.ts
The FROM address is: Brief &lt;noreply@briefnews.online&gt; (verified domain on Resend)

Mail Tester covers: Gmail web screenshots (free tier), spam score, SPF/DKIM/DMARC check.
Outlook desktop: Must be tested by sending to a real Outlook/Hotmail/Microsoft account.
Apple Mail: Send to an Apple Mail account (iCloud address) and open on macOS or iOS.
Gmail mobile: Open the Mail Tester email (or a direct send) in Gmail app on phone.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    No code changes in this plan. This is a manual email client testing checkpoint.

    The briefing email template (src/emails/briefing-email.tsx) was built in Phase 5 and used in production dispatches. This test verifies it renders correctly across all four required clients.
  </what-built>
  <how-to-verify>
    **Part A — Mail Tester (Gmail web + spam score)**

    1. Open https://mail-tester.com in a browser. Copy the unique test address shown (format: `test-{hash}@mail-tester.com`).

    2. Trigger a real briefing email send to that address. Options:
       - Option A (easiest): Temporarily update your delivery email address to the Mail Tester address in the DB, wait for the next cron run.
       - Option B (immediate): Use the Resend dashboard "Send Test Email" feature with the BriefingEmail template, setting recipient to the Mail Tester address.
       - Option C: Run a one-off script: `npx tsx -e "import 'dotenv/config'; import { sendBriefingEmail } from './src/lib/email'; sendBriefingEmail('test-hash@mail-tester.com', 'Tester', [{topicName:'Formula 1', items:[{headline:'Test', summary:'Test summary.', articleUrl:'https://example.com', sourceName:'BBC'}]}], 'Saturday, 7 March 2026').then(console.log)"`

    3. Return to mail-tester.com and click "Check your score".

    Expected: Score >= 9/10. SPF, DKIM, DMARC all green. Note any content warnings but the rendering match is the pass criterion, not the spam score alone.

    **Part B — Apple Mail**

    Send a briefing email to an iCloud address (your own). Open it in Apple Mail on macOS or iOS.
    Expected: Fonts load (Libre Baskerville or fallback serif), topic sections readable, source links clickable.

    **Part C — Outlook desktop**

    Send a briefing email to a Microsoft account (outlook.com or hotmail.com address). Open in Outlook desktop app.
    Expected: Layout intact, no broken images (template is text-only so this should be fine), topic headings visible.

    **Part D — Gmail mobile**

    Open the Mail Tester email (or a direct Gmail send) in Gmail on a phone or tablet.
    Expected: Text readable at mobile font size, no layout breakage.

    **Pass bar:** Visual output closely matches the React Email template design intent. Dark mode is out of scope — light mode only.
  </how-to-verify>
  <resume-signal>Type "approved" if all four clients render acceptably, or describe which client failed and what was broken</resume-signal>
</task>

</tasks>

<verification>
All four email clients render the BriefingEmail template with acceptable visual fidelity. Mail Tester score >= 9/10. Confirmed by developer checkpoint.
</verification>

<success_criteria>
Gate criterion 1 signed off: email renders correctly in Gmail (web + mobile), Apple Mail, and Outlook desktop. Mail Tester spam score >= 9/10.
</success_criteria>

<output>
After completion, create `.planning/phases/07-beta-polish/07-04-SUMMARY.md`
</output>
