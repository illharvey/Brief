---
phase: 07-beta-polish
task: 07-05 (AI hallucination audit — in progress, blocked on descriptions fix)
status: in_progress
last_updated: 2026-03-09T15:19:03.086Z
---

<current_state>
Mid-way through 07-05 (AI hallucination audit). We fixed the summarisation hallucination prompt and the ingestion pipeline to capture article descriptions, but article descriptions are still 0 in the DB. The root cause is that existing articles were ingested before the fix, and the BBC articles being tested are old stories no longer in the current RSS feed — so `onConflictDoUpdate` never fires for them. Need to either: (a) ingest with fresh articles that will pick up descriptions, or (b) investigate why the ingestion run we did (`run-ingestion.ts`) didn't backfill any descriptions despite onConflictDoUpdate.
</current_state>

<completed_work>

**Plans fully done:**
- 07-01 ✓ — Structured JSON logging in `src/lib/ingestion/index.ts` and `src/lib/summarisation/index.ts`
- 07-02 ✓ — Admin scripts: `scripts/briefing-count.ts` and `scripts/audit-briefing.ts`

**Plans with checkpoint pending:**
- 07-03 — `/unsubscribed` page created and E2E unsubscribe flow VERIFIED (page renders, DB row confirmed, token flow works). Just needs continuation agent to write SUMMARY.md.
- 07-04 — Email client testing: Mail Tester scored 8.2/10 (only deduction is `.online` TLD penalty, DKIM/SPF/DMARC all pass). User accepted this. Just needs continuation agent to write SUMMARY.md.

**Bug fixes made during session (all committed and pushed):**
- `fix(ci)`: curl `-L` flag for cron dispatch 308 redirect
- `fix`: `APP_URL` default changed from `brief.app` → `briefnews.online` in both `route.ts` and `email.ts`
- `fix`: Removed duplicate `/unsubscribed` page that conflicted with `(auth)/unsubscribed`
- `fix(summarisation)`: Prompt updated to prevent hallucination (no extrapolation, no placeholders, no meta-commentary, title-only detection)
- `fix(ingestion)`: `description` field threaded through all source adapters (RSS contentSnippet, NewsData description, Guardian trailText) → `RawArticle` type → `persist.ts` → DB
- `fix(ingestion)`: `onConflictDoUpdate` to backfill description on re-ingestion (replacing `onConflictDoNothing`)

**Plans not started:** 07-05 (blocked), 07-06 (blocked on 07-05)
</completed_work>

<remaining_work>

**Immediate blocker — descriptions not backfilling:**
- Articles in DB have `description: null` (all 547 articles)
- `onConflictDoUpdate` is in place but the articles being tested are old BBC stories no longer in current RSS feed
- Need to: wait for next cron run (15 min) to see fresh articles with descriptions, OR investigate why last ingestion run didn't update any rows

**07-05 — AI hallucination audit (remaining):**
- Generate a briefing using articles that have descriptions (fresh ingestion)
- Run `npx tsx --env-file=.env.local scripts/audit-briefing.ts <new-briefingId>`
- Review 10+ non-cached items with source snapshots, mark PASS/FAIL/UNVERIFIABLE
- All must PASS (zero fabricated facts) to sign off gate criterion 4

**07-03 checkpoint continuation (pending):**
- Spawn continuation agent for plan 07-03 with response "approved" — unsubscribe flow verified
- Spawn continuation agent for plan 07-04 with response "approved" — email rendering accepted

**07-06 — BETA-CHECKLIST.md:**
- After 07-05 passes, run executor agent for plan 07-06 (autonomous)
- Writes `.planning/BETA-CHECKLIST.md` with evidence from all gate criteria

**Then:** Run gsd-verifier, mark phase complete, update ROADMAP.md
</remaining_work>

<decisions_made>

- **Mail Tester 8.2/10 accepted** — only penalty is `.online` TLD being flagged as abused by SpamAssassin. DKIM/SPF/DMARC all pass. User decided to live with it for beta.
- **APP_URL** — production URL is `https://brief-umber.vercel.app` (Vercel deployment). `briefnews.online` domain not yet pointed to Vercel. Need to set `APP_URL=https://brief-umber.vercel.app` in Vercel env vars.
- **Summarisation approach for title-only sources** — when `sourceSnapshot === article.title` (no description), inject explicit user-turn instruction. System prompt alone was insufficient.
- **Description backfill via onConflictDoUpdate** — updates description column on re-ingestion conflict rather than skipping. Correct approach, just hasn't fired yet because old articles aren't in current feeds.
</decisions_made>

<blockers>

- **Article descriptions still null** — ingestion fix is deployed but old articles aren't being re-fetched (no longer in current RSS feeds). Workaround: wait for next cron cycle to accumulate fresh articles with descriptions, then generate a new briefing to audit against those.
- **07-03 and 07-04 SUMMARY.md not written** — checkpoint agents returned but never got continuation to write summaries. Need to handle this when resuming (spawn continuation agents with "approved").
</blockers>

<context>
Phase 7 is a hardening/verification phase. Five gate criteria:
1. ✓ Email renders in 4 clients — accepted (8.2/10 Mail Tester, TLD issue only)
2. ✓ Unsubscribe flow E2E — verified (page exists, DB row confirmed)
3. ✓ Pipeline error logging structured JSON — done in 07-01
4. ⏳ AI hallucination audit — in progress, blocked on getting fresh articles with descriptions
5. ✓ Observability (logging + daily metric) — done in 07-01 + 07-02

The hallucination fix uncovered a deeper architectural issue: RSS ingestion was never storing article descriptions, leaving the LLM to work from headlines only. Fixed at the ingestion layer. Once fresh articles flow through (next cron or manual re-ingest), the audit should work properly.

Also discovered and fixed: APP_URL hardcoded to wrong domain, duplicate /unsubscribed page causing build failures, cron dispatch not following redirects.
</context>

<next_action>
1. Check if any articles now have descriptions: `cat > /tmp/check.ts << 'EOF'\nimport 'dotenv/config'; import { db } from './src/lib/db/client'; import { articles } from './src/lib/db/schema'; import { isNotNull } from 'drizzle-orm'; async function main() { const r = await db.select().from(articles).where(isNotNull(articles.description)).limit(3); console.log(r.length, 'with desc'); r.forEach(x => console.log(x.description?.slice(0,80))); process.exit(0); } main();\nEOF\nnpx tsx --env-file=.env.local /tmp/check.ts`
2. If yes: flush Redis cache, generate new briefing, run audit script on new ID
3. If no: trigger cron endpoint manually: `curl -H "Authorization: Bearer $CRON_SECRET" $APP_URL/api/cron/dispatch` to ingest fresh articles
4. Then handle 07-03 and 07-04 checkpoint continuations (both "approved")
5. Then run 07-06 executor
</next_action>
