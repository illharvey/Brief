---
phase: 07-beta-polish
task: 4
total_tasks: 6
status: in_progress
last_updated: 2026-03-11T08:48:43.840Z
---

<current_state>
Plans 07-04 and 07-05 are unexecuted. 07-04 (email client QA) was skipped by the user — Mail Tester done (8.2/10), not yet checked in Apple Mail, Outlook desktop, Gmail mobile — deferred. 07-05 (hallucination audit) is blocked waiting on a fresh briefing generated with the new facts-only prompt. Redis cache is empty (0 cached summaries). The dispatch cron fires at 14:00 Europe/London — that will produce today's briefing with the new prompt automatically.
</current_state>

<completed_work>

**Plans formally completed:**
- 07-01: Delivery timezone fix ✓ (SUMMARY.md exists)
- 07-02: Article descriptions backfilled ✓ (SUMMARY.md exists)
- 07-03: /unsubscribed confirmation page ✓ (SUMMARY.md exists)

**This session (ad-hoc, deployed to prod):**
- Article body enrichment pipeline — `src/lib/ingestion/enrich.ts` (JSDOM + Readability, batch 5, 10s timeout, 200-char min, 50k cap)
- `EnrichmentResult` added to `types.ts`
- `enrichForUser()` wired into `dispatch.ts` (ingest → enrich → generate)
- `scripts/enrich-articles.ts` dev script
- Migrated LLM: `@google/generative-ai` → `openai` SDK → OpenRouter (`https://openrouter.ai/api/v1`)
- Default model: `google/gemini-2.0-flash-lite`; env: `OPENROUTER_API_KEY`
- Fixed `summarisation/index.ts` key guard (was GEMINI_API_KEY)
- Fixed Lambda crash — lazy `await import('jsdom')` inside function (static top-level import crashes Vercel Lambda at module init)
- Fixed `refreshBriefingAction` — now runs full ingest → enrich → generate (was only generate → blank briefing)
- Revised SYSTEM_PROMPT — facts-only extraction with concrete good/bad examples
- `maxDuration = 300` on dashboard page (covers server action timeout; cannot export const from "use server" files)
- `next.config.ts` serverExternalPackages attempt (superseded by lazy import fix)
</completed_work>

<remaining_work>

- **07-04**: Email client QA — still need Apple Mail, Outlook desktop, Gmail mobile verification. Mail Tester done (8.2/10). User deferred.
- **07-05**: Hallucination audit — need today's briefing (14:00 dispatch). Run `scripts/audit-briefing.ts <briefingId>` and review 10+ items.
- **07-06**: Create `BETA-CHECKLIST.md` — autonomous, depends on 07-01 through 07-05 signed off.
- Write SUMMARY.md files for 07-04 and 07-05 once signed off.
</remaining_work>

<decisions_made>

- Lazy import for jsdom/readability is the correct pattern for Vercel Lambda (Turbopack ignores serverExternalPackages for these)
- maxDuration must go on route/page file, NOT "use server" action file
- OpenRouter key: `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`
- User ID: `17c8fb84-5464-4034-ac44-a5bdf20bc64a`
- Mail Tester 8.2/10 accepted (.online TLD penalty)
</decisions_made>

<blockers>

- **Reddit RSS 403**: Reddit blocks non-browser RSS fetches. All reddit-rss sources fail.
- **Guardian API key**: `.env.local` has placeholder — returns 401. Check Vercel env vars.
- **Hallucination audit**: Needs today's 14:00 dispatch briefing. Run audit after it lands.
</blockers>

<context>
All infrastructure is done and deployed. Remaining plans are QA gates, not code. The 14:00 dispatch is the first real test of enrichment + facts-only prompt together. After that briefing arrives, audit it, sign off 07-05, then write BETA-CHECKLIST (07-06) and the phase is complete.
</context>

<next_action>
After 14:00 dispatch runs, get the new briefing ID:

```
DOTENV_CONFIG_PATH=.env.local npx tsx -e "import 'dotenv/config'; import { db } from './src/lib/db/client'; import { briefings } from './src/lib/db/schema'; import { eq, desc } from 'drizzle-orm'; async function main() { const r = await db.select({ id: briefings.id, generatedAt: briefings.generatedAt, itemCount: briefings.itemCount }).from(briefings).where(eq(briefings.userId, '17c8fb84-5464-4034-ac44-a5bdf20bc64a')).orderBy(desc(briefings.generatedAt)).limit(3); console.log(r); process.exit(0) } main()"
```

Then audit: `DOTENV_CONFIG_PATH=.env.local npx tsx scripts/audit-briefing.ts <briefingId> > audit.md`

Open audit.md — check each summary against its source snapshot for hallucinated facts.
</next_action>
