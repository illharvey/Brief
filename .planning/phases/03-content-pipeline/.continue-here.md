---
phase: 03-content-pipeline
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-02T10:32:26.097Z
---

<current_state>
Phase 3 Wave 3, Plan 03-04, Task 3 — human-verify checkpoint.
Tasks 1 and 2 are committed and code is complete. We're blocked on the human e2e verification: user needs to sign up via the app, then run two curl commands against /api/dev/ingest to confirm articles are fetched and dedup works.

The dev server is running (npm run dev on localhost:3000). The user is in the middle of signing up to get a userId.

A runtime fix was also applied this session: switched db client from neon-http to neon-serverless (commit 499087e) to fix "No transactions support in neon-http driver" error that appeared during signup at the topics step.
</current_state>

<completed_work>

**Phase 3 all waves complete except final human checkpoint:**

- Plan 03-01 ✓ — articles table (url+userId unique), RawArticle/IngestionResult types, contentHash()/normaliseUrl() dedup helpers, insertArticles() with onConflictDoNothing
- Plan 03-02 ✓ — fetchRssSource() with BBC_RSS_FEEDS, findSubredditsForTopic()/fetchRedditRss()/fetchRedditHot() with custom User-Agent and self-post filtering
- Plan 03-03 ✓ — fetchGuardianApi() and fetchNewsdataApi() adapters (no NewsAPI.org — production ToS issue, Guardian needs paid plan so key is placeholder)
- Plan 03-04 Tasks 1+2 ✓ — ingestForUser(userId) orchestrator (per-source try/catch, returns {fetched,inserted,skipped,errors[]}), POST /api/dev/ingest route (returns 403 in production)

**Fixes applied this session (outside GSD plans):**
- Switched db client from neon-http → neon-serverless (commit 499087e) — neon-http doesn't support db.transaction() which saveTopicsAction uses
- Added DATABASE_URL (real Neon connection string) to .env.local
- Added NEWSDATA_API_KEY to .env.local (pub_1237c09cf78f42879f81c83415257433)
- GUARDIAN_API_KEY is still placeholder (Guardian API is paid)

**Phase 2 still has open items:**
- DNS verification (02-03) pending — GoDaddy records added but propagation not yet confirmed
- RESEND_WEBHOOK_SECRET and UNSUBSCRIBE_SECRET not set in .env.local
- Phase 2 VERIFICATION.md status is gaps_found — cannot mark phase complete until DNS verified

</completed_work>

<remaining_work>

- Complete signup flow in browser (topics + delivery time step was erroring before neon-serverless fix — should work now)
- Get userId from Neon console or drizzle-kit studio
- Run first curl to POST /api/dev/ingest → confirm fetched > 0, inserted > 0
- Run second curl → confirm inserted = 0 (dedup works)
- Check articles table in Neon for source_name and source_url populated
- Type "approved" to unblock checkpoint
- Agent will create 03-04-SUMMARY.md and close the plan
- Run phase verifier → expect human_needed (GUARDIAN_API_KEY placeholder)
- Mark Phase 3 complete (or with known gaps)

</remaining_work>

<decisions_made>

- No Guardian API — requires paid plan, left as placeholder in .env.local. Pipeline still works via NewsData.io + BBC RSS + Reddit.
- No NewsAPI.org — free tier prohibits production use (research finding)
- Reuters RSS not used — Reuters discontinued RSS in 2020
- neon-serverless driver replaces neon-http — transactions required for saveTopicsAction atomic topic replace
- Reddit hot topics included as a source — surfaces trending linked articles from subreddits
- Per-user dedup: (url, userId) unique constraint — same article can appear for different users

</decisions_made>

<blockers>

- GUARDIAN_API_KEY: Guardian API requires paid plan. Pipeline runs without it (Guardian adapter throws, caught by per-source isolation, logged in errors[]). Not blocking for development.
- Phase 2 DNS: GoDaddy records added, propagation pending. Come back and type "dns-verified" once MXToolbox SPF/DKIM/DMARC all pass.
- Phase 2 env vars still needed: RESEND_WEBHOOK_SECRET and UNSUBSCRIBE_SECRET missing from .env.local.

</blockers>

<context>
We're in a "get it running" moment — the code is all done for Phase 3, we just need to verify it works against the live Neon database. The user is new to this stack (just set up Neon for the first time this session) so steps need plain-English explanation.

Phase 2 DNS is a parallel track — it'll close itself when GoDaddy propagates. The user added the DNS records, just waiting.

The neon-http → neon-serverless fix is a common Neon gotcha and worth noting in any future phases that use transactions.
</context>

<next_action>
Resume by asking the user if the signup now works (neon-serverless fix was just applied). If yes, help them find their userId via Neon console (console.neon.tech → Tables → users table → copy id). Then run the two curl commands to verify ingestion. Once approved, resume the checkpoint agent (agentId: a47dee3194078986d) or spawn a fresh continuation.
</next_action>
