---
phase: 06-web-app
plan: "04"
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified: []
autonomous: false
requirements: [PREF-04, WEB-01, WEB-02, WEB-03, WEB-04]

must_haves:
  truths:
    - "All 6 phase success criteria confirmed in live browser"
    - "No TypeScript errors in final build"
    - "No Next.js middleware deprecation warning in build output"
  artifacts:
    - path: ".planning/phases/06-web-app/06-04-SUMMARY.md"
      provides: "Human-verified completion record for Phase 6"
      contains: "PASS"
  key_links:
    - from: "human verification"
      to: "all 6 phase success criteria"
      via: "browser testing"
      pattern: "approved"
---

<objective>
Human verification of the complete Phase 6 web app. Confirm all 6 phase success criteria in a live browser before closing the phase.

Purpose: Phase 6 delivers significant UI surfaces — landing page, briefing viewer, archive, topic suggestions. Visual and interactive verification is required before marking the phase complete.
Output: Human approval that all 6 criteria pass.
</objective>

<execution_context>
@/Users/willharveynats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willharveynats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-web-app/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TypeScript build and start dev server</name>
  <files></files>
  <action>
Run the TypeScript check and build to confirm no errors before human verification:
```bash
cd /Users/willharveynats/conductor/repos/brief && npx tsc --noEmit
```
Must exit 0. If it fails, fix all TypeScript errors before proceeding.

Then confirm the dev server starts cleanly:
```bash
npm run dev
```
Check the dev server output for any warnings. Specifically look for:
- No "[next] middleware.ts has been renamed" deprecation warning (proxy.ts migration in Plan 01 should eliminate this)
- No compilation errors

Report the build result before requesting human verification.
  </action>
  <verify>
    <automated>cd /Users/willharveynats/conductor/repos/brief && npx tsc --noEmit && echo "TypeScript PASS"</automated>
  </verify>
  <done>TypeScript exits 0. Dev server starts without deprecation warnings or errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 6 web app:
- Plan 01: proxy.ts migration, Libre Baskerville font, ticker keyframe, signup email pre-fill, extractBriefingHeadline utility
- Plan 02: Briefing viewer replacing /dashboard placeholder, 14-day archive list, individual /dashboard/briefings/[id] route, topic suggestion pills, addTopicAction
- Plan 03: Public landing page at / with all 8 sections, CSS ticker, email CTA routing to /signup?email=...
  </what-built>
  <how-to-verify>
Start dev server: `npm run dev` in /Users/willharveynats/conductor/repos/brief

**Criterion 1 (WEB-01) — Read today's briefing:**
- Navigate to http://localhost:3000/dashboard (logged in)
- Confirm: briefing content renders with topic section headers (## headings styled as bold text), inline source attribution links in beeswax color
- OR confirm: "Your briefing is on the way" placeholder shows if no briefing has been generated today

**Criterion 2 (WEB-02) — Browse archive:**
- Confirm: "Past briefings" section below the viewer shows a list of past briefings (or empty state "Your first briefing arrives tomorrow")
- If briefings exist: click any past briefing row → confirm it navigates to /dashboard/briefings/[id] and renders the briefing

**Criterion 3 (PREF-03/WEB-03) — Settings page (already complete from Phase 5.1):**
- Click "Settings" in the sidebar → confirm /dashboard/settings loads correctly (unchanged from Phase 5.1)

**Criterion 4 (WEB-04) — Public landing page:**
- Open a private/incognito window (or sign out) and navigate to http://localhost:3000
- Confirm: landing page renders with all 8 sections: nav (Brief. logo + links), hero (heading + email form), ticker (scrolling strip), how-it-works (3 steps), topics grid (12 cards with emojis), testimonial quote, bottom CTA, footer
- Confirm: ticker scrolls automatically via CSS animation (not JS)
- Confirm: nav links are hidden on mobile viewport (resize browser to <768px wide)

**Criterion 5 (WEB-04 CTA) — Email pre-fill routing:**
- On the landing page, type an email in the hero or bottom CTA input (e.g. "test@brief.app")
- Click "Get Brief." or "Start Reading"
- Confirm: browser navigates to /signup?email=test%40brief.app
- Confirm: the email field on the signup page is pre-filled with "test@brief.app"

**Criterion 6 (PREF-04) — Topic suggestions:**
- Navigate to /dashboard while logged in
- Confirm: topic suggestion pills appear below the briefing content (or "Your briefing is on the way" area)
- Confirm: suggestions shown are NOT topics you already follow
- Click an "Add" pill → confirm: toast appears ("Topic added"), pill disappears

**Bonus check — auth redirect:**
- While logged in, navigate to http://localhost:3000 → confirm: redirected to /dashboard (landing page not shown to authenticated users)
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 criteria pass. Or describe which criteria failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
All 6 phase success criteria confirmed by human in live browser.
TypeScript build exits 0.
No Next.js deprecation warnings in dev server output.
</verification>

<success_criteria>
- Criterion 1: User can read today's briefing (or see "on the way" placeholder) at /dashboard — WEB-01
- Criterion 2: Archive list renders past briefings; individual briefing route works — WEB-02
- Criterion 3: /dashboard/settings loads correctly (Phase 5.1 unchanged) — WEB-03
- Criterion 4: Landing page at / shows all 8 sections to unauthenticated visitors — WEB-04
- Criterion 5: CTA email input routes to /signup?email=... and pre-fills the signup form — WEB-04
- Criterion 6: Topic suggestion pills appear, filter already-followed topics, add with toast — PREF-04
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-app/06-04-SUMMARY.md` following the summary template.
</output>
